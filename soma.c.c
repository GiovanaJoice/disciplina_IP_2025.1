#include <stdio.h>

int main() {
    int num1 = 5; // primeiro número
    int num2 = 7; // segundo número
    int soma;     // variável para armazenar a soma

    soma = num1 + num2; // realiza a soma

    printf("A soma de %d e %d é %d\n", num1, num2, soma);

    return 0;
}
